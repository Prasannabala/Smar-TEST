/**
 * AUTO-GENERATED by the LEGO block recording converter.
 * Source recording: recordings/example-todo.recording.ts
 *
 * To regenerate:
 *   npm run convert recordings/example-todo.recording.ts
 *
 * To run:
 *   npx playwright test tests/generated/example-todo.spec.ts
 */

import { test } from '@playwright/test';
import { DataDrivenRunner } from '../../src/runner/DataDrivenRunner';
import type { TestDataFile } from '../../src/runner/DataDrivenRunner';
import testData from './example-todo.data.json';

// Import block files to register them with BlockRegistry
import '../../src/blocks/actions/NavigateBlock';
import '../../src/blocks/actions/FillBlock';
import '../../src/blocks/actions/ClickBlock';
import '../../src/blocks/assertions/VisibleBlock';

const runner = new DataDrivenRunner();

for (const scenario of (testData as unknown as TestDataFile).scenarios) {
  test(scenario.name, async ({ page }) => {
    await runner.run(page, scenario.steps);
  });
}
